## Authoring model

Use markdown files for most content and reserve inline Svelte components for interactive callouts.

## Structure

- Group related pages under folders.
- Add a `_meta.json` file per folder for navigation order and label control.
