## Why SVOCS

SVOCS combines markdown-first authoring with Svelte 5-powered interactivity.

## Quick start

1. Add markdown files under `content/`.
2. Use `_meta.json` files to control navigation labels and ordering.
3. Start the dev server with `npm run dev`.

## Next steps

Continue with the architecture page and then add custom theme components under `src/lib/themes`.
